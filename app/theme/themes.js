"use client";
import { createTheme } from "@mui/material/styles";
export const darkTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
        main: "#191919",
    },
    secondary: {
        main: '#FFA600',
    }
  },
});